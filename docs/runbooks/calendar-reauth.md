# Calendar reauth

**Not an incident** unless widespread.

Host revoked Google access. Connection marked invalid; banner in dashboard. User reconnects OAuth. Busy blocks may be stale until full sync. Bookings remain valid.
